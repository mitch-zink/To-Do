This is a MEAN stack web application. To compile, you need to install dependencies in the frontend and api folder. Also, be sure to have the MEAN applications already installed. After that, serve the angular application from the front end folder. Then, in the api folder run node app.js. The application will now be fully functional.
